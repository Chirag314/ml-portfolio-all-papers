seed: 42

data:
  n_samples: 1000
  n_features: 5
  noise_std: 0.5
  batch_size: 64
  val_split: 0.2

model:
  n_features: 5

train:
  epochs: 100
  lr: 0.05
  optimizer: "sgd"     # "gd" (full batch) or "sgd"
  log_interval: 10
  ckpt_path: "outputs/regression_linear.pt"
